/* LANDING PAGE
 *
 * What is it doing?
 * =================
 *   Takes the SVG  draws the path, then zooms in and hides the container.
 *
 *
 * Variables
 * ==========
 *   get the animation container
 *   get the tub div
 *
 * Logic
 * ======
 *   run the animation on the svg
 *   when the animation finishes, rotate the image
 *       when the image reaches 55deg zoom in
 *       then make the background transform fade
 *       then hide the initial container
 *
 *
 * AUTHOR: Gerald Wells <gerald.endeavor@gmail.com>
 */

// Variables
const container = document.getElementById('animation-container');
const tubPath = document.getElementByClassName('outline');


tubPath.setTimeout(function () {

}, 10);
// Functions
drawPath.    // draws the svg path

const rotateDiv = () => {

} // rotates the image on the x axis

const zoomDiv = () => {

} // zooms in on the div then hides the container
